# deob Benchmark

Quantify deob's impact on LLM-based JavaScript reverse engineering through controlled experiments.

## Dependencies

```bash
# Required for obfuscation
npm install javascript-obfuscator
```

## Quick Start

```bash
# 1. To start from an existing experiment, copy it first:
cp -r experiments/<source> experiments/$(date -I)

# 2. Prepare scenarios: write original.js and ground-truth.json
#    - Write original.js with clean business logic
#    - Write ground-truth.json with expected answers
#    Place files in experiments/<date>/scenarios/<X>/

# 3. Generate obfuscated code
node experiments/<date>/tools/obfuscate.js

# 4. Run deob on all scenarios
node experiments/<date>/tools/runner.js

# 5. Spawn sub-agents for comparison (see Agent Prompt Template)
#    - Agent A reads deob output (0-prompt.md → 2-index.txt → main.js)
#    - Agent B reads obfuscated.js directly
#    - Save answers to results/scenario_<X>_deob.json and **_raw.json

# 6. Embed token/time metadata into answer JSONs
node -e "..."  # or manually add _meta: { tokens, time } to each answer JSON

# 7. Run LLM scoring for qualitative dimensions
node experiments/<date>/tools/score-llm.js     # generates prompt
# → Send prompt to LLM (Claude/GPT) → save response as results/llm-scores.json

# 8. Score results (combines LLM + rule-based)
node experiments/<date>/tools/score.js --all

# 9. Generate charts
node experiments/<date>/tools/gen-charts.js

# 8. Write report at experiments/<date>/report.md
```

## File Structure

```
benchmark/
  README.md                           ← this file
  experiments/                         ← all experiment runs
    2026-07-17-v1/                    ← one experiment (date-based naming)
      imgs/							  ← experiment charts
      	pie.svg
      	bar.svg
      	radar.svg
      	...
      tools/                           ← experiment scripts
        obfuscate.js                   ← generate obfuscated.js from original.js
        runner.js                      ← run deob on all scenarios
        score.js                       ← compare agent answers to ground truth
        gen-charts.js                  ← generate SVG charts
        score-llm.js                   ← generate LLM scoring prompt
      scenarios/                       ← experiment scenarios
        A/                             ← scenario A
          original.js                  ← original source code
          obfuscated.js                ← obfuscated version (generated by obfuscate.js)
          ground-truth.json            ← expected answers for scoring
        B/
        C/
        ...
      results/                         ← experiment output
        deob-output/                   ← deob output directories
          scenario_A/                  ← deob output for scenario A
          ...
        scenario_A_deob.json          ← Agent A answer (with _meta: tokens, time)
        scenario_A_raw.json           ← Agent B answer (with _meta: tokens, time)
        ...
        llm-scores.json               ← LLM-judged qualitative dimension scores
        llm-score-prompt.txt          ← generated scoring prompt
      report.md                       ← experiment report
```

## Scenario Requirements

Each scenario needs three files:

### 1. original.js

Clean, readable JavaScript code with meaningful business logic. Should include:
- Functions with clear purposes
- Call relationships between functions
- At least one "interesting" aspect (API call, security issue, complex logic)

### 2. ground-truth.json

Structured answers for scoring. Like this format:

```json
{
  "scenario": "A",
  "difficulty": "easy|medium|hard",
  "description": "one sentence summarizing the code's purpose",
  "obfuscationTechniques": ["list of techniques used"],
  "functions": [
    {
      "name": "functionName",
      "purpose": "what it does (1 sentence)",
      "params": ["param1", "param2"],
      "returns": "return type description",
      "calls": ["fn1", "fn2"],
      "calledBy": ["fn3"],
      "security": ["security concern if any"]
    }
  ],
  "callGraph": {
    "functionName": ["list of functions it calls"]
  },
  "dataFlow": [
    "step 1 → step 2 → step 3"
  ],
  "securityIssues": [
    {"severity": "high|medium|low", "issue": "description", "location": "function name"}
  ],
  "apiEndpoints": [
    {"method": "GET|POST|PUT|DELETE", "path": "/api/endpoint", "purpose": "description"}
  ],
  "keyVariables": {
    "VARIABLE_NAME": "value or purpose"
  }
}
```

### 3. obfuscated.js

Generated by `tools/obfuscate.js`. Edit `obfuscate.js` to configure obfuscation options per scenario.

## Obfuscation Configuration

Edit `tools/obfuscate.js` to configure `javascript-obfuscator` options per scenario:

```javascript
const scenarios = {
  A: {
    name: "Scenario Name",
    difficulty: "easy",
    options: {
      renameGlobals: true,
      stringArray: true,
      stringArrayEncoding: ["base64"],  // or "rc4"
      controlFlowFlattening: true,
      controlFlowFlatteningThreshold: 0.5,  // 0.0-1.0
      deadCodeInjection: true,
      deadCodeInjectionThreshold: 0.2,
      selfDefending: true,
      debugProtection: true,
      splitStrings: true,
      numbersToExpressions: true,
      transformObjectKeys: true,
      unicodeEscapeSequence: true,
    }
  },
};
```

Key options and their impact:
| Option | Effect | Best for |
|--------|--------|----------|
| stringArray + stringArrayEncoding | Hides string literals | All scenarios |
| controlFlowFlattening | Reorders branches into switch-case | Medium/Hard |
| deadCodeInjection | Adds unreachable code blocks | Medium/Hard |
| selfDefending | Anti-tampering checks | Hard |
| debugProtection | DevTools detection | Hard |

## Agent Prompt Template

Use this template when spawning sub-agents:

**Agent A (deob)**:
```
RE task: Analyze deobfuscated JavaScript code.

READ IN ORDER:
1. {deob_output_dir}/0-prompt.md
2. {deob_output_dir}/2-index.txt (full file)
3. {deob_output_dir}/main.js (focus on banners)

Answer:
{
  "purpose": "one sentence",
  "functions": [{"name":"...","purpose":"...","calls":[...],"calledBy":[...]}],
  "endpoints": [{"method":"...","path":"...","purpose":"..."}],
  "security": [{"severity":"high|medium|low","issue":"..."}],
  "dataFlow": "input → processing → output",
  "variables": [{"name":"...","value":"..."}],
  "entryPoint": "...",
  "functionCount": 0
}
JSON only, no markdown.
```

**Agent B (raw)**:
```
RE task: Analyze OBFUSCATED JavaScript code directly.

READ: {scenario_dir}/obfuscated.js

Answer: (same JSON format as Agent A)
JSON only, no markdown.
```

Save agent answers as:
- `results/scenario_{X}_deob.json` (Agent A)
- `results/scenario_{X}_raw.json` (Agent B)

After saving, add `_meta` with token/time data to each:
```json
{
  "_meta": { "tokens": 15000, "time": 30 },
  "purpose": "...",
  "functions": [...]
}
```

## Scoring

```bash
# Score specific scenarios
node tools/score.js A B

# Score all scenarios
node tools/score.js --all

# Override experiment dir
BENCH_EXP_DIR=/path/to/experiment node tools/score.js --all
```

### Scoring Weights

For example, 9 indexes and calculation:

| Dimension  | Weight | Method | Formula / Description                                        |
| ---------- | ------ | ------ | ------------------------------------------------------------ |
| Functions  | 30%    | LLM    | Semantic match of purpose + call relationships against ground truth |
| Security   | 20%    | LLM    | Conceptual match: "weak hash" = "algorithm is trivially reversible" |
| Endpoints  | 15%    | Rule   | $\frac{\text{matched endpoints}}{\text{truth endpoints}}$ (exact method + fuzzy path) |
| DataFlow   | 10%    | LLM    | Semantic overlap of data flow description                    |
| Variables  | 10%    | LLM    | Value/purpose matching regardless of variable naming         |
| Purpose    | 5%     | LLM    | Semantic match of one-sentence summary                       |
| Time       | 5%     | Rule   | $1 - \frac{t_{\text{agent}}}{t_{\text{deob}} + t_{\text{raw}}}$ |
| EntryPoint | 2.5%   | Rule   | $\begin{cases}1 & \text{if identified}\\0 & \text{otherwise}\end{cases}$ |
| Token      | 2.5%   | Rule   | $1 - \frac{k_{\text{agent}}}{k_{\text{deob}} + k_{\text{raw}}}$ |

## Generate the charts

After scoring, run `gen-charts.js` to produce SVG chart images in `imgs/`:

```bash
node experiments/<date>/tools/gen-charts.js
```

| File | Type | Used in |
|------|------|---------|
| `imgs/bar-total.svg` | Bar chart | Executive Summary |
| `imgs/bar-time.svg` | Bar chart | Efficiency section |
| `imgs/bar-token.svg` | Bar chart | Token section |
| `imgs/pie-value.svg` | Donut chart | Aggregate Analysis |
| `imgs/radar-A.svg` ~ `radar-E.svg` | Radar chart | Per-scenario sections |
## Writing the Report

Use this framework for `report.md`. Keep each section concise — data drives the narrative.

```
# Benchmark Report · YYYY-MM-DD

**N scenarios · N runs · scoring method**

## 1. Executive Summary
- One-sentence takeaway (bold quote)
- Key numbers table (Total score, Endpoints, Functions, Security)

## 2. Experiment Design
- Goal (one line)
- Method (one paragraph)
- Scoring weights (table)
- Scenario table: # | Domain | Level | Techniques

## 3. Per-Scenario Results
For each scenario:
  ### X. Name (Difficulty)
  - One-line result: deob Nx | key finding
  - Radar chart + score table (all 9 dimensions, deob vs raw)
  - What happened (2-3 sentences)
  - Why deob helped/didn't (1-2 sentences)

## 4. Aggregate Analysis
- By Difficulty
- By Dimension
- By Technique

## 5. Key Findings
- 4-6 bullet points: what deob does best + where it struggles

## 6. Limitations
- 3-5 known issues with methodology or scoring

## 7. Conclusions
- Where deob matters most / least
- Next steps for deob project
```
